import { ArrowRight, CheckCircle2, HeartHandshake, ShieldCheck, WalletCards } from 'lucide-react';
import { Link } from '@inertiajs/react';
import PublicPageShell from '../Components/PublicPageShell';
import { usePublicData } from '../Components/usePublicData';
const fallback={image:'https://images.unsplash.com/photo-1469571486292-0ba58a3f068b?auto=format&fit=crop&w=2200&q=88'};
const steps=[['Proposal','A welfare need is documented and a support proposal is prepared.',HeartHandshake],['Review & approval','The proposal follows the configured review and approval workflow.',ShieldCheck],['Member obligations','Approved support creates transparent member obligations rather than automatic deductions.',WalletCards],['Collection & closure','Payments are recorded, balances are visible, and the event is closed with an auditable record.',CheckCircle2]];
export default function Welfare(){const data=usePublicData('welfare',fallback);return <PublicPageShell eyebrow="Welfare" title={<>Support people with <span className="text-emerald-500">structure and care.</span></>} intro="Welfare should be compassionate and accountable at the same time. The platform is designed to make support decisions, obligations, collections, and balances easy to follow." image={String(data.image)}><section className="mx-auto max-w-7xl px-5 pb-16 sm:px-6 lg:px-8 lg:pb-24"><div className="grid gap-4 lg:grid-cols-4">{steps.map(([title,text,Icon],i)=>{const I=Icon as typeof HeartHandshake;return <article key={title as string} className="relative rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><span className="absolute right-5 top-5 text-[10px] font-black text-slate-300">0{i+1}</span><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-rose-50 text-rose-600"><I size={21}/></div><h2 className="mt-5 text-base font-black text-slate-950">{title as string}</h2><p className="mt-2 text-sm leading-6 text-slate-500">{text as string}</p></article>})}</div><div className="mt-6 grid gap-6 lg:grid-cols-[1fr_.8fr]"><div className="rounded-[2rem] border border-slate-200 bg-white p-7 shadow-sm sm:p-9"><p className="text-xs font-black uppercase tracking-[0.18em] text-emerald-600">Example welfare event</p><h2 className="mt-3 text-2xl font-black text-slate-950">Bereavement support</h2><p className="mt-3 text-sm leading-7 text-slate-600">A chairman may propose a support amount, but the amount is not automatically deducted. Once approved, the system can create member obligations and show who is fully covered, needs a top-up, or has no balance.</p><div className="mt-6 grid gap-3 sm:grid-cols-3">{['Fully covered','Top-up required','No balance'].map(x=><div key={x} className="rounded-xl bg-slate-50 px-4 py-3 text-xs font-bold text-slate-700">{x}</div>)}</div></div><div className="rounded-[2rem] border border-emerald-100 bg-emerald-50/70 p-7 sm:p-9"><h2 className="text-xl font-black text-slate-950">Transparent by design.</h2><p className="mt-3 text-sm leading-7 text-emerald-950/70">Whether existing member balances may be applied can be controlled by the organization’s approved policy rather than hidden in code.</p><Link href="/contact" className="mt-6 inline-flex items-center gap-2 rounded-xl bg-slate-950 px-5 py-3 text-sm font-extrabold text-white hover:bg-emerald-600">Learn about welfare <ArrowRight size={16}/></Link></div></div></section></PublicPageShell>}
