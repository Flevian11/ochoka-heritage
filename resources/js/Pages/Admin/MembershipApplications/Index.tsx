import { Head, Link, router } from '@inertiajs/react';
export default function Index({applications,filters,statuses}:{applications:any,filters:{status:string},statuses:string[]}){return <><Head title="Membership applications"/><main className="min-h-screen bg-slate-50"><div className="mx-auto max-w-6xl px-6 py-8"><Link href="/admin" className="text-sm font-semibold text-emerald-700">← Dashboard</Link><div className="mt-3 flex items-end justify-between gap-4"><div><h1 className="text-3xl font-bold text-slate-900">Membership applications</h1><p className="mt-1 text-slate-500">Review applications before creating association memberships.</p></div><select value={filters.status||''} onChange={e=>router.get('/admin/membership-applications',e.target.value?{status:e.target.value}:{},{preserveState:true})} className="rounded-xl border border-slate-300 bg-white px-4 py-3"><option value="">All statuses</option>{statuses.map(s=><option key={s} value={s}>{s.replace('_',' ')}</option>)}</select></div><div className="mt-6 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50"><tr><th className="px-5 py-3">Application</th><th className="px-5 py-3">Applicant</th><th className="px-5 py-3">Contact</th><th className="px-5 py-3">Status</th><th className="px-5 py-3"></th></tr></thead><tbody>{applications.data.map((a:any)=><tr key={a.id} className="border-t border-slate-100"><td className="px-5 py-4 font-semibold">{a.application_number}</td><td className="px-5 py-4">{[a.first_name,a.middle_name,a.last_name].filter(Boolean).join(' ')}</td><td className="px-5 py-4">{a.phone}<br/>{a.email}</td><td className="px-5 py-4 capitalize">{a.status.replace('_',' ')}</td><td className="px-5 py-4 text-right"><Link href={`/admin/membership-applications/${a.id}`} className="font-semibold text-emerald-700">Review</Link></td></tr>)}</tbody></table></div></div></main></>}
