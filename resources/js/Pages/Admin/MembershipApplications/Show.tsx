import { FormEvent, useState } from 'react';
import { Head, Link, router } from '@inertiajs/react';
export default function Show({application}:{application:any}){const [reason,setReason]=useState('');const post=(url:string)=>router.post(url);const reject=(e:FormEvent)=>{e.preventDefault();router.post(`/admin/membership-applications/${application.id}/reject`,{reason});};return <><Head title={`Application ${application.application_number}`}/><main className="min-h-screen bg-slate-50"><div className="mx-auto max-w-5xl px-6 py-8"><Link href="/admin/membership-applications" className="text-sm font-semibold text-emerald-700">← Applications</Link><div className="mt-4 grid gap-6 lg:grid-cols-3"><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm lg:col-span-2"><div className="flex items-start justify-between"><div><h1 className="text-2xl font-bold text-slate-900">{application.application_number}</h1><p className="mt-1 capitalize text-slate-500">{application.status.replace('_',' ')}</p></div></div><div className="mt-7 grid gap-5 md:grid-cols-2">{[['Name',[application.first_name,application.middle_name,application.last_name].filter(Boolean).join(' ')],['Phone',application.phone],['Email',application.email],['Date of birth',application.date_of_birth],['National ID',application.national_id],['Location',[application.city,application.county].filter(Boolean).join(', ')],['Address',application.address]].map(([k,v])=><div key={k as string}><div className="text-xs font-bold uppercase tracking-wide text-slate-400">{k}</div><div className="mt-1 text-slate-800">{v||'—'}</div></div>)}</div></section><aside className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-bold text-slate-900">Actions</h2>{['submitted'].includes(application.status)&&<button onClick={()=>post(`/admin/membership-applications/${application.id}/review`)} className="mt-4 w-full rounded-xl border border-slate-300 px-4 py-3 font-semibold">Start review</button>}{['submitted','under_review'].includes(application.status)&&<><button onClick={()=>post(`/admin/membership-applications/${application.id}/approve`)} className="mt-3 w-full rounded-xl bg-emerald-700 px-4 py-3 font-bold text-white">Approve & create member</button><form onSubmit={reject} className="mt-5"><textarea value={reason} onChange={e=>setReason(e.target.value)} placeholder="Reason for rejection" rows={4} className="w-full rounded-xl border border-slate-300 p-3"/><button className="mt-2 w-full rounded-xl border border-red-300 px-4 py-3 font-semibold text-red-700">Reject application</button></form></>}</aside></div></div></main></>}
