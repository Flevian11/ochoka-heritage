import { ArrowRight, CheckCircle2, FileText, MessageCircle, UserRound, Users } from 'lucide-react';
import { Link } from '@inertiajs/react';
import PublicPageShell from '../Components/PublicPageShell';
import { usePublicData } from '../Components/usePublicData';
const fallback={image:'https://images.unsplash.com/photo-1529156069898-49953e39b3ac?auto=format&fit=crop&w=2200&q=88'};
const benefits=[['Member profile','A structured identity record for participation and community information.',UserRound],['Participation','Stay connected to notices, meetings, welfare events, and community activity.',Users],['Communication','Receive relevant community updates through configured notification channels.',MessageCircle],['Personal records','Access contribution, welfare, document, and activity information available to you.',FileText]];
export default function Membership(){const data=usePublicData('membership',fallback);return <PublicPageShell eyebrow="Membership" title={<>A clearer home for <span className="text-emerald-500">every member.</span></>} intro="Membership is the foundation of the platform. Each person has one member identity, with access and responsibilities shaped by their current role and approved participation." image={String(data.image)}><section className="mx-auto max-w-7xl px-5 pb-16 sm:px-6 lg:px-8 lg:pb-24"><div className="grid gap-4 md:grid-cols-2">{benefits.map(([title,text,Icon])=>{const I=Icon as typeof UserRound;return <article key={title as string} className="rounded-2xl border border-slate-200 bg-white p-7 shadow-sm"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-sky-50 text-sky-600"><I size={21}/></div><h2 className="mt-5 text-lg font-black text-slate-950">{title as string}</h2><p className="mt-2 text-sm leading-6 text-slate-500">{text as string}</p></article>})}</div><div className="mt-6 rounded-[2rem] border border-emerald-100 bg-emerald-50/70 p-7 sm:p-10"><h2 className="text-2xl font-black text-slate-950">Rules should be configurable, not buried in the interface.</h2><p className="mt-3 max-w-2xl text-sm leading-7 text-emerald-950/70">Live membership eligibility is not connected yet, so this public page uses explanatory demo content. Once the organization settings module exists, these rules can be supplied dynamically.</p><Link href="/contact" className="mt-6 inline-flex items-center gap-2 rounded-xl bg-slate-950 px-5 py-3 text-sm font-extrabold text-white hover:bg-emerald-600">Ask about membership <ArrowRight size={16}/></Link></div><div className="mt-7 grid gap-3 sm:grid-cols-3">{['One member identity','Role-based access','Auditable member history'].map(x=><div key={x} className="flex items-center gap-2.5 rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm font-bold text-slate-700"><CheckCircle2 size={17} className="text-emerald-500"/>{x}</div>)}</div></section></PublicPageShell>}
