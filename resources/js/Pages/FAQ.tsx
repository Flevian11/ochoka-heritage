import { ChevronDown, HelpCircle } from 'lucide-react';
import { useState } from 'react';
import PublicPageShell from '../Components/PublicPageShell';
import { usePublicData } from '../Components/usePublicData';
const fallback={image:'https://images.unsplash.com/photo-1521737711867-e3b97375f902?auto=format&fit=crop&w=2200&q=88'};
const faqs=[['Who can become a member?','Membership eligibility will be controlled by organization settings. The current public build uses explanatory demo content because the live membership policy module has not yet been connected.'],['How are contributions handled?','Contribution rules and amounts are intended to be configurable and effective-dated. Payments can initially be recorded manually, with payment-provider integration added later.'],['Does welfare automatically deduct money?','No. An approved welfare proposal creates member support obligations. It does not silently deduct funds.'],['Can leadership change without losing records?','Yes. Executive appointments are separate from membership. Ending an appointment removes executive privileges while preserving the member and historical records.'],['How are elections handled?','The planned workflow supports nomination, verification, secret voting, results verification, publication, and archival with voter eligibility snapshots.'],['How do meetings work?','Meetings are organizational records. Google Meet can later provide the actual online call while the platform stores the meeting, attendance, minutes, decisions, and actions.']];
export default function FAQ(){const data=usePublicData('faq',fallback);const[open,setOpen]=useState<number|null>(0);return <PublicPageShell eyebrow="FAQ" title={<>Questions, answered <span className="text-emerald-500">clearly.</span></>} intro="A simple public guide to how Ochoka Heritage is intended to work. As modules become connected to live organization data, these answers can be managed from administration." image={String(data.image)}><section className="mx-auto max-w-4xl px-5 pb-16 sm:px-6 lg:pb-24">{faqs.map(([q,a],i)=>{const active=open===i;return <article key={q} className="mb-3 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><button type="button" onClick={()=>setOpen(active?null:i)} className="flex w-full items-center justify-between gap-5 px-5 py-5 text-left sm:px-6"><span className="flex items-center gap-3 text-sm font-black text-slate-900 sm:text-base"><span className="flex h-9 w-9 shrink-0 items-center justify-center rounded-xl bg-emerald-50 text-emerald-600"><HelpCircle size={17}/></span>{q}</span><ChevronDown size={18} className={`shrink-0 text-slate-400 transition ${active?'rotate-180':''}`}/></button>{active&&<div className="border-t border-slate-100 px-5 pb-6 pt-4 text-sm leading-7 text-slate-600 sm:px-6">{a}</div>}</article>})}</section></PublicPageShell>}
